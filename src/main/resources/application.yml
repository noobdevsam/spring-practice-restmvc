spring:
    application:
        name: SpringPracticeRESTMVC
    jpa:
        show-sql: true
    profiles:
        active: localdb
    h2:
        console:
            enabled: true
    datasource:
        url: jdbc:h2:mem:testdb


logging:
    level:
        com:
            example: debug